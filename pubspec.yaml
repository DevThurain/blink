name: blink
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  args: ^2.4.2   # for parsing command line arguments
  path: ^1.9.0  # file system paths
  io: ^1.0.4    # optional: file utils, progress bars
  mason: ^0.1.0 # optional: templates like mason bricks
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

executables:
  blink: blink